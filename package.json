{
  "name": "x1pays",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev:fac": "pnpm -C packages/facilitator dev",
    "dev:api": "pnpm -C packages/api dev",
    "dev": "concurrently -k \"pnpm dev:fac\" \"pnpm dev:api\"",
    "build": "pnpm -C packages/facilitator build && pnpm -C packages/api build && pnpm -C packages/sdk build",
    "test": "pnpm -C packages/facilitator test && pnpm -C packages/api test && pnpm -C packages/sdk test",
    "lint": "eslint .",
    "fmt": "prettier -w ."
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.3.0",
    "typescript": "^5.6.0"
  }
}
